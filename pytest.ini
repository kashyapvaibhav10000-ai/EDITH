[pytest]
addopts = -m "not live_service and not integration"
markers =
    live_service: tests that require a running local EDITH service on 127.0.0.1
    integration: slower tests that exercise multiple EDITH modules together
