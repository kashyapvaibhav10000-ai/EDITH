# handlers/ — per-intent handler modules for EDITH
#
# helpers.py        — extraction + safety helpers
# local_exec.py     — local system/file op detection (_run_local_exec)
# email.py          — email, unread_email
# shell.py          — shell, create_file, delete_file, file_query
# calendar.py       — calendar_today, calendar_week, calendar_create
# search.py         — search, lookup, weather
# phone.py          — call, sms, phone, whatsapp
# memory_handler.py — rag, profile, briefing
# system.py         — wake, session_end, system_health
# mcp.py            — mcp
# misc.py           — vision, open_app, data_analysis, agent, council,
#                     decision, morning_briefing, self_improve, image_gen,
#                     video_summarize, repo_analyze, chat_fallback
# meta.py           — identity, compact, think_level, trace_toggle,
#                     agent_stop, list_skills
# pending_action.py — HITL execute_pending_action
